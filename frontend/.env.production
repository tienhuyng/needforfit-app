# Production API base URL (no secrets — safe to commit)
# GitHub Actions builds with this URL via workflow env.
# Override at build time: docker build --build-arg VITE_API_URL=...

VITE_API_URL=https://training.withkris.life/api
